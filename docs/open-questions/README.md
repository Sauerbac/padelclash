# Open questions

Design questions deferred for autonomous resolution, one file per question
(`NN-slug.md`). The `rating-engine.md` / `data-model.md` references to
"open-question 03" (the ≥3-competitive-match ranked threshold) are **already
decided** — the threshold ships as `group.ranked_threshold` default 3. This
directory exists so future deferrals have a home and the cross-references resolve.
