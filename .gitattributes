# Normalize to LF in the repo and on checkout everywhere. Critical for shell
# scripts that run inside the Linux container (a CRLF shebang would break them).
* text=auto eol=lf

# Binary assets git should never touch.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
