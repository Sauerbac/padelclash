// Tailwind v4 is the single source of truth for design tokens (ADR-0009).
// The @theme block lives in src/app/globals.css.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
